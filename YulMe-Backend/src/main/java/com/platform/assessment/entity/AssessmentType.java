package com.platform.assessment.entity;

/** Mirrors the {@code assessment_type} CHECK constraint on {@code assessments} (V16 migration). */
public enum AssessmentType {

    PLACEMENT,
    PERIODIC,
    DIAGNOSTIC

}