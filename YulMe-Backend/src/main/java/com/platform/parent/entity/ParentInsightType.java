package com.platform.parent.entity;

/** Mirrors the {@code insight_type} CHECK constraint on {@code parent_insights} (V19 migration). */
public enum ParentInsightType {

    PROGRESS,
    WEAKNESS,
    STRENGTH,
    RECOMMENDATION

}
