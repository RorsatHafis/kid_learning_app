package com.platform.learner.entity;

/** Mirrors the {@code status} CHECK constraint on {@code learner_objective_states} (V15 migration). */
public enum LearnerObjectiveStatus {

    NOT_STARTED,
    IN_PROGRESS,
    MASTERED

}