package com.platform.learner.entity;

/** Mirrors the {@code triggered_by_type} CHECK constraint on {@code learner_skill_history} (V22 migration). */
public enum EvidenceSource {

    ACTIVITY_ATTEMPT,
    ASSESSMENT_ATTEMPT

}