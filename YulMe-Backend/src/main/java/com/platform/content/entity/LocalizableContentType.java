package com.platform.content.entity;

/** Mirrors the {@code content_type} CHECK constraint on {@code content_localizations} (V12 migration). */
public enum LocalizableContentType {

    LESSON_VERSION,
    ACTIVITY_VERSION,
    QUESTION_VERSION

}