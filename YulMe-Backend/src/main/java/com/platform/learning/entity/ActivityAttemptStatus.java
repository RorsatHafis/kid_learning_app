package com.platform.learning.entity;

/** Mirrors the {@code status} CHECK constraint on {@code activity_attempts} (V14 migration). */
public enum ActivityAttemptStatus {

    IN_PROGRESS,
    COMPLETED,
    ABANDONED

}