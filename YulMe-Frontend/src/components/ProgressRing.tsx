export default function ProgressRing({value,size=108,label='Progress'}:{value:number;size?:number;label?:string}){const r=(size-12)/2,c=2*Math.PI*r,d=(value/100)*c;return <div className="ring" style={{width:size,height:size}}><svg viewBox={`0 0 ${size} ${size}`}><circle className="ring-bg" cx={size/2} cy={size/2} r={r}/><circle className="ring-fill" cx={size/2} cy={size/2} r={r} strokeDasharray={`${d} ${c}`} /></svg><div className="ring-copy"><strong>{value}%</strong><span>{label}</span></div></div>}
